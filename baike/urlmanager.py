# coding=utf-8root_url = 'http://baike.baidu.com/item/Python'class UrlManager(object):    def __init__(self):        self.url_new = set()        self.url_crawled = set()        self.url_new.add(root_url)    def hasNext(self):        print len(self.url_new)        return len(self.url_new) != 0    def getNext(self):        next_url = self.url_new.pop()        self.url_crawled.add(next_url)        return next_url    def add_new_urls(self, new_urls):        if new_urls is None:            return        for url in new_urls:            self.add_new_url(url)    def add_new_url(self, new_url):        if new_url is None:            return        if new_url in self.url_new:            return        if new_url in self.url_crawled:            return            self.url_new.add(new_url)